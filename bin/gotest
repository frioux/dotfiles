#!/bin/sh

test() {
   date
   echo =================================================
   go test -v ./...
   echo =================================================
}

if [ "$1" = "test" ]; then
   test
else
   test
   minotaur . -- "$0" test
fi

