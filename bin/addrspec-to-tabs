#!/usr/bin/env perl

use 5.20.0;
use warnings;

use Email::Address;

while (<STDIN>) {
   chomp;
   my @addrs = Email::Address->parse($_);
   say join "\t", $_->address, $_->phrase || '', $_->comment || ''
      for @addrs;
}
