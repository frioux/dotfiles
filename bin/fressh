#!/bin/dash

ssh $1 <<SH
mkdir -p \$HOME/code
if [ ! -d \$HOME/code/dotfiles ]; then
   git clone --quiet --recursive git://github.com/frioux/dotfiles \$HOME/code/dotfiles
   cd \$HOME/code/dotfiles
   ./install.sh
fi
SH

exec ssh $1

# vim: ft=sh
