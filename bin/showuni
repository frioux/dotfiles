#!/bin/zsh

perl -Mcharnames=:full -E'my $str = shift; binmode(STDOUT, ":encoding(UTF-8)"); print charnames::string_vianame(uc $str)' "$(alluni.pl | prepend-emoji-hist ~/.uni_history | rofi -dmenu)" | store-hist ~/.uni_history | xsel -i
