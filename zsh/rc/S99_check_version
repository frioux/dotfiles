if [ $(date +%s) -gt $(cat ~/.dotfilecheck) ]; then
   rm ~/.dotfilecheck
   echo $(($(date +%s)+60*60*24)) > ~/.dotfilecheck
   cd ~/code/dotfiles
   git pull --ff-only
   mk
fi


# vim: ft=zsh foldmethod=marker
