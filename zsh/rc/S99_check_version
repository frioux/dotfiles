zmodload zsh/datetime

if [[ $EPOCHSECONDS -gt $(cat ~/.dotfilecheck) ]]; then
   rm ~/.dotfilecheck
   echo $(($EPOCHSECONDS+60*60*24)) > ~/.dotfilecheck
   cd ~/code/dotfiles
   git pull --ff-only
   mk
fi


# vim: ft=zsh foldmethod=marker
